#include "collider.hpp"

bool collisionDetector(const Collidible &a, const Collidible &b) {
  a.getPosition().x <= b.getPosition().x + b.getWidth()

                                               return
}
